// PCSP Variable Declarations (placeholder)
// Replace with your sport-specific state variables.
//
// In tennis, these are: court positions (f_ad_ct, f_de_ct, ...),
// player enum, score counters, ball location, and points-to-win.

enum{state_a, state_b};
enum{entity1, entity2, na};

var turn = na;
var score1 = 0;
var score2 = 0;
var won = na;
var target_score = 7;
